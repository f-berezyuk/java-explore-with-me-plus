CREATE TABLE IF NOT EXISTS endpoint_hit_entity (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    app VARCHAR(255),
    uri VARCHAR(255),
    ip VARCHAR(255),
    timestamp TIMESTAMP WITHOUT TIME ZONE NOT NULL,
    CONSTRAINT pk_endpoint_hit_entity PRIMARY KEY (id)
);
